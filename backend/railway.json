{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../frontend && npm install && npm run build && xcopy dist ..\\backend\\public /E /I /Y && cd ../backend && npm install"
  },
  "deploy": {
    "startCommand": "node server.js"
  }
}